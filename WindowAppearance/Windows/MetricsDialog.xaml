<Window x:Class="GmmImageSegmentator.MetricsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Статистика кластеризации" Width="500"
        WindowStartupLocation="CenterOwner" 
        WindowStyle="ToolWindow" Height="452">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Метрики качества кластеризации" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

        <!-- Блок Custom -->
        <GroupBox Grid.Row="1" Header="Custom GMM" Margin="0,5">
            <TextBlock x:Name="CustomMetricsText" Margin="5" FontFamily="Consolas"/>
        </GroupBox>

        <!-- Блок Accord -->
        <GroupBox Grid.Row="2" Header="Accord.NET GMM" Margin="0,5">
            <TextBlock x:Name="AccordMetricsText" Margin="5" FontFamily="Consolas"/>
        </GroupBox>

        <!-- Памятка -->
        <GroupBox Grid.Row="3" Header="Как интерпретировать" Margin="0,5" Height="142" Cursor="Help">
            <TextBlock TextWrapping="Wrap" Margin="5">
                <Bold>BIC / AIC:</Bold> чем <Bold>меньше</Bold>, тем лучше модель сочетает точность и простоту.
                <LineBreak/>
                <Bold>Log-Likelihood:</Bold> чем <Bold>больше</Bold>, тем выше правдоподобие данных при данной модели.
                <LineBreak/>
                <Bold>Silhouette Score:</Bold> находится в диапазоне [-1, 1]. Значения ближе к <Bold>1</Bold> означают более компактные и хорошо разделимые кластеры.
            </TextBlock>
        </GroupBox>
    </Grid>
</Window>