<Window x:Class="GmmImageSegmentator.ComparisonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сравнение результатов" Height="600" Width="1200"
        WindowStartupLocation="CenterOwner" 
        Background="{DynamicResource BackgroundBrush}" 
        Foreground="{DynamicResource ForegroundBrush}" 
        WindowStyle="ToolWindow">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовки -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Текущее изображение" 
                   FontWeight="Bold" HorizontalAlignment="Center" Margin="5"/>
        <TextBlock Grid.Row="0" Grid.Column="2" Text="Accord.NET GMM" 
                   FontWeight="Bold" HorizontalAlignment="Center" Margin="5"/>

        <!-- Рамка с изображением (слева) -->
        <Border Grid.Row="1" Grid.Column="0" 
                BorderBrush="{DynamicResource ControlBorderBrush}" 
                BorderThickness="2" CornerRadius="8" 
                Background="{DynamicResource ControlBackgroundBrush}"
                Margin="5">
            <Image x:Name="CustomImageControl" 
                   Stretch="Uniform" 
                   StretchDirection="Both"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>
        </Border>

        <!-- Рамка с изображением (справа) -->
        <Border Grid.Row="1" Grid.Column="2" 
                BorderBrush="{DynamicResource ControlBorderBrush}" 
                BorderThickness="2" CornerRadius="8" 
                Background="{DynamicResource ControlBackgroundBrush}"
                Margin="5">
            <Image x:Name="AccordImageControl" 
                   Stretch="Uniform" 
                   StretchDirection="Both"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>
        </Border>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" 
                    Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Использовать Custom" Click="UseCustom_Click" 
                    Width="150" Margin="0,0,10,0"/>
            <Button Content="Использовать Accord" Click="UseAccord_Click" 
                    Width="150"/>
        </StackPanel>
    </Grid>
</Window>